export class Quote {
    //  describe: boolean;
    id: number;
    name: string;
    quote: string;
    author: string;

    //constructor(id: number, name: string, quote: string, author: string) {
    // this.describe = false;

    //}
}
